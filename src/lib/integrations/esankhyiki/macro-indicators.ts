export async function getMacroIndicators() {
  return {
    available: false,
    warning:
      "Macro indicator series are not invented. Import official CSV/API rows when a permitted download or documented endpoint is configured.",
  };
}
